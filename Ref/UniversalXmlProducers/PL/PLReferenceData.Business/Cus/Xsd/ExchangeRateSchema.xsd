<?xml version="1.0" encoding="utf-8"?>
<xs:schema id="ExchangeRateSchema"
    targetNamespace="http://tempuri.org/ExchangeRateSchema.xsd"
    elementFormDefault="qualified"
    xmlns="http://tempuri.org/ExchangeRateSchema.xsd"
    xmlns:mstns="http://tempuri.org/ExchangeRateSchema.xsd"
    xmlns:xs="http://www.w3.org/2001/XMLSchema"
>
	<xs:element name="tabela_kursow">
		<xs:complexType>
			<xs:sequence>
				<xs:element name="numer_tabeli" type="xs:string" />
				<xs:element name="data_publikacji" type="xs:date" />
				<xs:element maxOccurs="unbounded" name="pozycja">
					<xs:complexType>
						<xs:sequence>
							<xs:element name="nazwa_waluty" type="xs:string" />
							<xs:element name="przelicznik" type="xs:decimal" />
							<xs:element name="kod_waluty" type="xs:string" />
							<xs:element name="kurs_sredni" type="xs:string" />
						</xs:sequence>
					</xs:complexType>
				</xs:element>
			</xs:sequence>
			<xs:attribute name="typ" type="xs:string" use="required" />
			<xs:attribute name="uid" type="xs:string" use="required" />
		</xs:complexType>
	</xs:element>

</xs:schema>
