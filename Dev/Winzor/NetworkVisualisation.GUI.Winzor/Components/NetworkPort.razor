@using CargoWise.NetworkVisualisation.GUI.Models
<div class="networkport__circle" @onmouseenter=@OnMouseEnter @onmouseleave=@OnMouseLeave title="@(Port.Type == NetworkPortModel.PortType.Input ? "Input" : "Output") Port"></div>
@if (CompatibilityIndicatorImage is not null)
{
	<img src=@CompatibilityIndicatorImage>
}
