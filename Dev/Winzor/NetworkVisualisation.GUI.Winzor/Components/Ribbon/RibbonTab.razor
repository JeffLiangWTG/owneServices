@namespace CargoWise.NetworkVisualisation.GUI

@if (ViewModel is not null)
{
    <div class="ribbontab">
        @foreach (var group in ViewModel.Groups)
        {
            <RibbonGroup ViewModel="group"></RibbonGroup>
        }
    </div>

}
