@namespace Enterprise.ZArchitecture.GUI.SearchBox
@using Microsoft.AspNetCore.Components.Web
@using MouseEventArgs = System.Windows.Forms.MouseEventArgs
@inherits CargoWise.Windows.UI.KUserControl

<div class="zsearchbox-results" @onmousedown="OnMouseDown" @onmousedown:preventDefault @onmousedown:stopPropagation style="@ControlStyleString" data-winzor-control-id="@WinzorControlId" data-type="@GetType()" data-name="@(Name ?? string.Empty)" title="@ToolTipText">
    @RenderControls()
</div>

@code {
    public override bool UseParentDivForLayout => false;

    void OnMouseDown()
    {
        // Empty method to enable prevent default
    }
}
