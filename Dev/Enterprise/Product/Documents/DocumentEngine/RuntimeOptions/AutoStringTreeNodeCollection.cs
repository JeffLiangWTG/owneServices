/* This file has been automatically generated by X-Code .NET -- DO NOT EDIT! */

/*
Enterprise.DocumentEngine.RuntimeOptions.AutoStringTreeNodeCollection
Enterprise.DocumentEngine.RuntimeOptions.AutoStringTreeNodeCollection.Enumerator

These C# classes implement a strongly-typed collection of 
StringTreeNode objects.

The internal representation is an array of StringTreeNode, so the performance 
characteristics will be more like a vector than a list, to use STL terminology.

The implementation is optimized for value-types, as it goes to great length to 
avoid the overhead of boxing and unboxing.  But it should also work well for 
reference types.

Mad props to Nick Wienholt, Kit George, Joel Mueller, Ethan Smith, Chris Sells, 
and Patrice Lafond for assisting me, at various times and in various ways.

Especially to Kit George, for his generous contribution to the dotnet mailing 
list -- a code-generator for CollectionBase-derived classes:
	http://discuss.develop.com/archives/wa.exe?A2=ind0107C&L=DOTNET&P=R35911
This was the original inspiration for the fine code you are now enjoying.

- Shawn A. Van Ness
*/

using System;
using System.Collections;

using T = Enterprise.DocumentEngine.RuntimeOptions.StringTreeNode;

namespace Enterprise.DocumentEngine.RuntimeOptions
{
	[Resources.AutoGeneratedSourceCode()]
	[Serializable]
	public class AutoStringTreeNodeCollection : ICollection, IList, IEnumerable, ICloneable
	{
		const int DefaultMinimumCapacity = 16;

		T[] m_array = new T[DefaultMinimumCapacity];
		int m_count;
		int m_version;

		// Construction

		public AutoStringTreeNodeCollection()
		{ }

		public AutoStringTreeNodeCollection(AutoStringTreeNodeCollection collection)
		{ AddRange(collection); }

		public AutoStringTreeNodeCollection(T[] array)
		{ AddRange(array); }

		// Operations (type-safe ICollection)

		public int Count
		{
			get
			{ return m_count; }
		}

		public void CopyTo(T[] array)
		{
			this.CopyTo(array, 0);
		}

		public void CopyTo(T[] array, int start)
		{
			if (m_count > array.GetUpperBound(0) + 1 - start)
			{
				throw new System.ArgumentException("Destination array was not long enough.");
			}

			// for (int i=0; i < m_count; ++i) array[start+i] = m_array[i];
			Array.Copy(m_array, 0, array, start, m_count);
		}

		// Operations (type-safe IList)

		public T this[int index]
		{
			get
			{
				ValidateIndex(index); // throws
				return m_array[index];
			}
			set
			{
				ValidateIndex(index); // throws

				++m_version;
				m_array[index] = value;
			}
		}

		public int Add(T item)
		{
			if (NeedsGrowth())
			{
				Grow();
			}

			++m_version;
			m_array[m_count] = item;

			return m_count++;
		}

		public void Clear()
		{
			++m_version;
			m_array = new T[DefaultMinimumCapacity];
			m_count = 0;
		}

		public bool Contains(T item)
		{
			return (IndexOf(item) != -1);
		}

		public int IndexOf(T item)
		{
			for (int i = 0; i < m_count; ++i)
			{
				if (m_array[i].Equals(item))
				{
					return i;
				}
			}

			return -1;
		}

		public void Insert(int position, T item)
		{
			ValidateIndex(position, true); // throws

			if (NeedsGrowth())
			{
				Grow();
			}

			++m_version;
			// for (int i=m_count; i > position; --i) m_array[i] = m_array[i-1];
			Array.Copy(m_array, position, m_array, position + 1, m_count - position);

			m_array[position] = item;
			m_count++;
		}

		public void Remove(T item)
		{
			int index = IndexOf(item);
			if (index < 0)
			{
				throw new System.ArgumentException("Cannot remove the specified item because it was not found in the specified Collection.");
			}

			RemoveAt(index);
		}

		public void RemoveAt(int index)
		{
			ValidateIndex(index); // throws

			++m_version;
			m_count--;
			// for (int i=index; i < m_count; ++i) m_array[i] = m_array[i+1];
			Array.Copy(m_array, index + 1, m_array, index, m_count - index);

			if (NeedsTrimming())
			{
				Trim();
			}
		}

		// Operations (type-safe IEnumerable)

		public Enumerator GetEnumerator()
		{
			return new Enumerator(this);
		}

		// Operations (type-safe ICloneable)

		public AutoStringTreeNodeCollection Clone()
		{
			AutoStringTreeNodeCollection tc = new AutoStringTreeNodeCollection();
			tc.AddRange(this);
			tc.Capacity = this.m_array.Length;
			tc.m_version = this.m_version;
			return tc;
		}

		// Public helpers (just to mimic some nice features of ArrayList)

		public int Capacity
		{
			get
			{ return m_array.Length; }
			set
			{
				if (value < m_count)
				{
					value = m_count;
				}

				if (value < DefaultMinimumCapacity)
				{
					value = DefaultMinimumCapacity;
				}

				if (m_array.Length == value)
				{
					return;
				}

				++m_version;

				T[] temp = new T[value];
				// for (int i=0; i < m_count; ++i) temp[i] = m_array[i];
				Array.Copy(m_array, 0, temp, 0, m_count);
				m_array = temp;
			}
		}

		public void AddRange(AutoStringTreeNodeCollection collection)
		{
			// for (int i=0; i < collection.Count; ++i) Add(collection[i]);

			++m_version;

			Capacity += collection.Count;
			Array.Copy(collection.m_array, 0, this.m_array, m_count, collection.m_count);
			m_count += collection.Count;
		}

		public void AddRange(T[] array)
		{
			// for (int i=0; i < array.Length; ++i) Add(array[i]);

			++m_version;

			Capacity += array.Length;
			Array.Copy(array, 0, this.m_array, m_count, array.Length);
			m_count += array.Length;
		}

		// Implementation (helpers)

		void ValidateIndex(int index)
		{
			ValidateIndex(index, false);
		}

		void ValidateIndex(int index, bool allowEqualEnd)
		{
			int max = (allowEqualEnd) ? (m_count) : (m_count - 1);
			if (index < 0 || index > max)
			{
				throw new System.ArgumentOutOfRangeException("Index was out of range.  Must be non-negative and less than the size of the collection.", index, "Specified argument was out of the range of valid values.");
			}
		}

		bool NeedsGrowth()
		{
			return (m_count >= Capacity);
		}

		void Grow()
		{
			if (NeedsGrowth())
			{
				Capacity = m_count * 2;
			}
		}

		bool NeedsTrimming()
		{
			return (m_count <= Capacity / 2);
		}

		void Trim()
		{
			if (NeedsTrimming())
			{
				Capacity = m_count;
			}
		}

		// Implementation (ICollection)

		/* redundant w/ type-safe method
		int ICollection.Count
		{
			get
			{ return m_count; }
		}
		*/

		bool ICollection.IsSynchronized
		{
			get
			{ return m_array.IsSynchronized; }
		}

		object ICollection.SyncRoot
		{
			get
			{ return m_array.SyncRoot; }
		}

		void ICollection.CopyTo(Array array, int start)
		{
			this.CopyTo((T[])array, start);
		}

		// Implementation (IList)

		bool IList.IsFixedSize
		{
			get
			{ return false; }
		}

		bool IList.IsReadOnly
		{
			get
			{ return false; }
		}

		object IList.this[int index]
		{
			get
			{ return this[index]; }
			set
			{ this[index] = (T)value; }
		}

		int IList.Add(object item)
		{
			return this.Add((T)item);
		}

		/* redundant w/ type-safe method
		void IList.Clear()
		{
			this.Clear();
		}
		*/

		bool IList.Contains(object item)
		{
			return this.Contains((T)item);
		}

		int IList.IndexOf(object item)
		{
			return this.IndexOf((T)item);
		}

		void IList.Insert(int position, object item)
		{
			this.Insert(position, (T)item);
		}

		void IList.Remove(object item)
		{
			this.Remove((T)item);
		}

		/* redundant w/ type-safe method
		void IList.RemoveAt(int index)
		{
			this.RemoveAt(index);
		}
		*/

		// Implementation (IEnumerable)

		IEnumerator IEnumerable.GetEnumerator()
		{
			return this.GetEnumerator();
		}

		// Implementation (ICloneable)

		object ICloneable.Clone()
		{
			return this.Clone();
		}

		// Nested enumerator class

		public class Enumerator : IEnumerator
		{
			readonly AutoStringTreeNodeCollection m_collection;
			int m_index;
			readonly int m_version;

			// Construction

			internal Enumerator(AutoStringTreeNodeCollection tc)
			{
				m_collection = tc;
				m_index = -1;
				m_version = tc.m_version;
			}

			// Operations (type-safe IEnumerator)

			public T Current
			{
				get
				{ return m_collection[m_index]; }
			}

			public bool MoveNext()
			{
				if (m_version != m_collection.m_version)
				{
					throw new System.InvalidOperationException("Collection was modified; enumeration operation may not execute.");
				}

				++m_index;
				return (m_index < m_collection.Count);
			}

			public void Reset()
			{
				if (m_version != m_collection.m_version)
				{
					throw new System.InvalidOperationException("Collection was modified; enumeration operation may not execute.");
				}

				m_index = -1;
			}

			// Implementation (IEnumerator)

			object IEnumerator.Current
			{
				get
				{ return this.Current; }
			}

			/* redundant w/ type-safe method
			bool IEnumerator.MoveNext()
			{
				return this.MoveNext();
			}
			*/

			/* redundant w/ type-safe method
			void IEnumerator.Reset()
			{
				this.Reset();
			}
			*/
		}
	}
}
