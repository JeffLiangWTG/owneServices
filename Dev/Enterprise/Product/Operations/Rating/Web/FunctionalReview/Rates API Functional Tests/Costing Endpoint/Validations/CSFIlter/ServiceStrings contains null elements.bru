meta {
  name: ServiceStrings contains null elements
  type: http
  seq: 16
}

post {
  url: {{BaseApplicationURL}}/api/rating/costing/{{BranchCode}}/{{DepartmentCode}}
  body: json
  auth: none
}

script:pre-request {
   await RatesAPIsHelper.SetAuthorizationForRequests();

   var query = RatesAPIsHelper.GetAValidRateQuery();
   query.CSFilter = { ServiceStrings: [null] }
   req.setBody(query);
}

tests {
  test("Should return status code 400", function() {
    expect(res.status).to.equal(400);
  });
  
  test("Should return: Provided ServiceStrings is not valid. It shouldn't contain null or empty elements.", function() {
    expect(res.body.ModelState['CSFilter.ServiceStrings'][0]).to.equal("Provided ServiceStrings is not valid. It shouldn't contain null or empty elements.");
  });
  
  
}
