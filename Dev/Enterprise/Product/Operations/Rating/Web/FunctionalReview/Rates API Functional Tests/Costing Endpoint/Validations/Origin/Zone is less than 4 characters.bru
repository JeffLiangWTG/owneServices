meta {
  name: Zone is less than 4 characters
  type: http
  seq: 15
}

post {
  url: {{BaseApplicationURL}}/api/rating/costing/{{BranchCode}}/{{DepartmentCode}}
  body: json
  auth: none
}

script:pre-request {
   await RatesAPIsHelper.SetAuthorizationForRequests();

   var query = RatesAPIsHelper.GetAValidRateQuery();
   query.Origin = { Type: "ZONE", Value: "AUZ" };
   req.setBody(query);
}

tests {
  test("Should return status code 400", function() {
    expect(res.status).to.equal(400);
  });
  
  test("Should return: Provided International Zone ('AUZ') is not valid. It should be a string of 4 characters.", function() {
    expect(res.body.ModelState['Origin.Value'][0]).to.equal("Provided International Zone ('AUZ') is not valid. It should be a string of 4 characters.");
  });
}
