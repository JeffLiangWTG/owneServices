meta {
  name: ServiceProviders contains empty objects
  type: http
  seq: 5
}

post {
  url: {{BaseApplicationURL}}/api/rating/costing/{{BranchCode}}/{{DepartmentCode}}
  body: json
  auth: none
}

script:pre-request {
   await RatesAPIsHelper.SetAuthorizationForRequests();

   var query = RatesAPIsHelper.GetAValidRateQuery();
   query.ServiceProviders = [{}];
   req.setBody(query);
}

tests {
  test("Should return status code 400", function() {
    expect(res.status).to.equal(400);
  });
  
  test("Should return: One and only one of the following properties must be specified for a service provider: 'CWCode', 'SCAC', 'IATACode' or 'C1CCode'.", function() {
    expect(res.body.ModelState['ServiceProviders[0]'][0]).to.equal("One and only one of the following properties must be specified for a service provider: 'CWCode', 'SCAC', 'IATACode' or 'C1CCode'.");
  });
  
  
}
