meta {
  name: TransportMode is null
  type: http
  seq: 2
}

post {
  url: {{BaseApplicationURL}}/api/rating/costing/{{BranchCode}}/{{DepartmentCode}}
  body: json
  auth: none
}

script:pre-request {
   await RatesAPIsHelper.SetAuthorizationForRequests();

   var query = RatesAPIsHelper.GetAValidRateQuery();
   query.TransportMode = null;
   req.setBody(query);
}

tests {
  test("Should return status code 400", function() {
    expect(res.status).to.equal(400);
  });
  
  test("Should return: TransportMode is Mandatory.", function() {
    expect(res.body.ModelState['TransportMode'][0]).to.equal("TransportMode is Mandatory.");
  });
}
