meta {
  name: IntercompanyTariffs Endpint - Postcode is not allowed
  type: http
  seq: 7
}

post {
  url: {{BaseApplicationURL}}/api/rating/intercompanytariffs/{{BranchCode}}/{{DepartmentCode}}
  body: json
  auth: none
}

script:pre-request {
   await RatesAPIsHelper.SetAuthorizationForRequests();

   var query = RatesAPIsHelper.GetAValidRateQueryForIntercompanyTariffs();
   query.RateOrigin = { Type: "POSTCODE", Value: "ABCDEFG" };
   req.setBody(query);
}

tests {
  test("Should return status code 400", function() {
    expect(res.status).to.equal(400);
  });
  
  test("Should return: Provided Type ('POSTCODE') is not valid. It only can be one of these values: 'COUNTRY', 'UNLOCO', 'ZONE'.", function() {
    expect(res.body.ModelState['RateOrigin.Type'][0]).to.equal("Provided Type ('POSTCODE') is not valid. It can only be one of these values: 'COUNTRY', 'UNLOCO', 'ZONE'.");
  });
  
  
  
}
