meta {
  name: Client can not be provided when querying Intercompanytariffs
  type: http
  seq: 1
}

post {
  url: {{BaseApplicationURL}}/api/rating/Intercompanytariffs/{{BranchCode}}/{{DepartmentCode}}
  body: json
  auth: none
}

script:pre-request {
   await RatesAPIsHelper.SetAuthorizationForRequests();

   var query = RatesAPIsHelper.GetAValidRateQueryForIntercompanyTariffs();
   query.Client = "";
   req.setBody(query);
   
}

tests {
  test("Should return status code 400", function() {
    expect(res.status).to.equal(400);
  });
  
  test("Should return: Client can not be provided when querying IntercompanyTariffs.", function() {
    expect(res.body.ModelState['Client'][0]).to.equal("Client can not be provided when querying IntercompanyTariffs.");
  });
}
