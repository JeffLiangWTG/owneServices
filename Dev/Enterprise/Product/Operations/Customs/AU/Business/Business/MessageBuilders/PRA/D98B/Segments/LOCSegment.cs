using Enterprise.Edifact.Auto;

namespace Enterprise.Customs.AU.Declaration.Business.D98B
{
	[Resources.AutoGeneratedSourceCode()]
	public class LOCSegment : Segment
	{
		public LOCSegment()
		{
			MaximumNumberOfValues = 6;
		}

		protected override string SegmentNameOverride
		{
			get { return "LOC"; }
		}

		public PlaceLocationQualifierList PlaceLocationQualifier;

		public readonly LocationIdentificationElements LocationIdentification = new LocationIdentificationElements();

		public readonly RelatedLocationOneIdentificationElements RelatedLocationOneIdentification = new RelatedLocationOneIdentificationElements();

		public readonly RelatedLocationTwoIdentificationElements RelatedLocationTwoIdentification = new RelatedLocationTwoIdentificationElements();

		public string RelationCoded;

		#region Implementation

		protected override void GetValues()
		{
			ValueItems = new object[]
			{
				PlaceLocationQualifier,
				LocationIdentification,
				RelatedLocationOneIdentification,
				RelatedLocationTwoIdentification,
				RelationCoded
			};
		}

		protected override void SetValues()
		{
			PlaceLocationQualifier = PlaceLocationQualifierList.GetFromString(Values(1));
			LocationIdentification.Parse(CharacterSet, Values(2));
			RelatedLocationOneIdentification.Parse(CharacterSet, Values(3));
			RelatedLocationTwoIdentification.Parse(CharacterSet, Values(4));
			RelationCoded = Values(5);
		}

		#endregion
	}
}
