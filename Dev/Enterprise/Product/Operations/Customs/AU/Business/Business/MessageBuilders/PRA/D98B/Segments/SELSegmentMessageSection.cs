using System;
using System.Collections;
using Enterprise.Edifact.Auto;

namespace Enterprise.Customs.AU.Declaration.Business.D98B
{
	[Resources.AutoGeneratedSourceCode()]
	public class SELSegmentMessageSection : MessageSectionBase, IEnumerable
	{
		public SELSegmentMessageSection(int maximumRepetitions) : base(maximumRepetitions)
		{
		}

		public override Type SectionType
		{
			get { return typeof(SELSegment); }
		}

		public SELSegment this[int index]
		{
			get { return (SELSegment)MySection[index]; }
		}

		IEnumerator IEnumerable.GetEnumerator()
		{
			return new SELSegmentEnumerator(this);
		}

		public SELSegment this[string value, int occurrence]
		{
			get { return (SELSegment)MySection[value, occurrence]; }
		}

		public SELSegment this[string value]
		{
			get { return this[value, 1]; }
		}

		public SELSegment InstantiateAChildAndAddItToChildrenCollection()
		{
			return (SELSegment)MySection.InstantiateAChildAndAddItToChildrenCollection();
		}

		#region SELSegmentEnumerator

		[Resources.AutoGeneratedSourceCode()]
		public class SELSegmentEnumerator : IEnumerator
		{
			int index;
			readonly SELSegmentMessageSection collection;
			public SELSegmentEnumerator(SELSegmentMessageSection collection)
			{
				this.collection = collection;
				index = -1;
			}

			public void Reset()
			{
				index = -1;
			}

			public bool MoveNext()
			{
				index++;
				return (index < collection.Children.Count);
			}

			public SELSegment Current
			{
				get { return (collection[index]); }
			}

			// The current property on the IEnumerator interface:
			object IEnumerator.Current
			{
				get { return (Current); }
			}
		}

		#endregion
	}
}
