using System;
using System.Collections;
using Enterprise.Edifact.Auto;

namespace Enterprise.Freight.Forwarding.DataTransfer
{
	[Resources.AutoGeneratedSourceCode()]
	public class RMXSHDSegmentMessageSection : MessageSectionBase, IEnumerable
	{
		public RMXSHDSegmentMessageSection(int maximumRepetitions) : base(maximumRepetitions)
		{
		}

		public override Type SectionType
		{
			get { return typeof(RMXSHDSegment); }
		}

		public RMXSHDSegment this[int index]
		{
			get { return (RMXSHDSegment)MySection[index]; }
		}

		IEnumerator IEnumerable.GetEnumerator()
		{
			return new SHDSegmentEnumerator(this);
		}

		public RMXSHDSegment this[string value, int occurrence]
		{
			get { return (RMXSHDSegment)MySection[value, occurrence]; }
		}

		public RMXSHDSegment this[string value]
		{
			get { return this[value, 1]; }
		}

		public RMXSHDSegment InstantiateAChildAndAddItToChildrenCollection()
		{
			return (RMXSHDSegment)MySection.InstantiateAChildAndAddItToChildrenCollection();
		}

		#region SHDSegmentEnumerator

		[Resources.AutoGeneratedSourceCode()]
		public class SHDSegmentEnumerator : IEnumerator
		{
			int Index;
			readonly RMXSHDSegmentMessageSection Collection;
			public SHDSegmentEnumerator(RMXSHDSegmentMessageSection collection)
			{
				this.Collection = collection;
				Index = -1;
			}

			public void Reset()
			{
				Index = -1;
			}

			public bool MoveNext()
			{
				Index++;
				return (Index < Collection.Children.Count);
			}

			public RMXSHDSegment Current
			{
				get { return (Collection[Index]); }
			}

			// The current property on the IEnumerator interface:
			object IEnumerator.Current
			{
				get { return (Current); }
			}
		}

		#endregion
	}
}
