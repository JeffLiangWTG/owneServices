using System;
using System.Collections;
using Enterprise.Edifact.Auto;

namespace Enterprise.Freight.Forwarding.DataTransfer
{
	[Resources.AutoGeneratedSourceCode()]
	public class RMISHDSegmentMessageSection : MessageSectionBase, IEnumerable
	{
		public RMISHDSegmentMessageSection(int maximumRepetitions) : base(maximumRepetitions)
		{
		}

		public override Type SectionType
		{
			get { return typeof(RMISHDSegment); }
		}

		public RMISHDSegment this[int index]
		{
			get { return (RMISHDSegment)MySection[index]; }
		}

		IEnumerator IEnumerable.GetEnumerator()
		{
			return new RMISHDSegmentEnumerator(this);
		}

		public RMISHDSegment this[string value, int occurrence]
		{
			get { return (RMISHDSegment)MySection[value, occurrence]; }
		}

		public RMISHDSegment this[string value]
		{
			get { return this[value, 1]; }
		}

		public RMISHDSegment InstantiateAChildAndAddItToChildrenCollection()
		{
			return (RMISHDSegment)MySection.InstantiateAChildAndAddItToChildrenCollection();
		}

		#region RMISHDSegmentEnumerator

		[Resources.AutoGeneratedSourceCode()]
		public class RMISHDSegmentEnumerator : IEnumerator
		{
			int Index;
			readonly RMISHDSegmentMessageSection Collection;
			public RMISHDSegmentEnumerator(RMISHDSegmentMessageSection collection)
			{
				this.Collection = collection;
				Index = -1;
			}

			public void Reset()
			{
				Index = -1;
			}

			public bool MoveNext()
			{
				Index++;
				return (Index < Collection.Children.Count);
			}

			public RMISHDSegment Current
			{
				get { return (Collection[Index]); }
			}

			// The current property on the IEnumerator interface:
			object IEnumerator.Current
			{
				get { return (Current); }
			}
		}

		#endregion
	}
}
