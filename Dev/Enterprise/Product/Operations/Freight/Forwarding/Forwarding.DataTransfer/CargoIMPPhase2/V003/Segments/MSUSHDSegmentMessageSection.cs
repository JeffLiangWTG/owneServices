using System;
using System.Collections;
using Enterprise.Edifact.Auto;

namespace Enterprise.Freight.Forwarding.DataTransfer
{
	[Resources.AutoGeneratedSourceCode()]
	public class MSUSHDSegmentMessageSection : MessageSectionBase, IEnumerable
	{
		public MSUSHDSegmentMessageSection(int maximumRepetitions) : base(maximumRepetitions)
		{
		}

		public override Type SectionType
		{
			get { return typeof(MSUSHDSegment); }
		}

		public MSUSHDSegment this[int index]
		{
			get { return (MSUSHDSegment)MySection[index]; }
		}

		IEnumerator IEnumerable.GetEnumerator()
		{
			return new MSUSHDSegmentEnumerator(this);
		}

		public MSUSHDSegment this[string value, int occurrence]
		{
			get { return (MSUSHDSegment)MySection[value, occurrence]; }
		}

		public MSUSHDSegment this[string value]
		{
			get { return this[value, 1]; }
		}

		public MSUSHDSegment InstantiateAChildAndAddItToChildrenCollection()
		{
			return (MSUSHDSegment)MySection.InstantiateAChildAndAddItToChildrenCollection();
		}

		#region MSUSHDSegmentEnumerator

		[Resources.AutoGeneratedSourceCode()]
		public class MSUSHDSegmentEnumerator : IEnumerator
		{
			int Index;
			readonly MSUSHDSegmentMessageSection Collection;
			public MSUSHDSegmentEnumerator(MSUSHDSegmentMessageSection collection)
			{
				this.Collection = collection;
				Index = -1;
			}

			public void Reset()
			{
				Index = -1;
			}

			public bool MoveNext()
			{
				Index++;
				return (Index < Collection.Children.Count);
			}

			public MSUSHDSegment Current
			{
				get { return (Collection[Index]); }
			}

			// The current property on the IEnumerator interface:
			object IEnumerator.Current
			{
				get { return (Current); }
			}
		}

		#endregion
	}
}
