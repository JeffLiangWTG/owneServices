# The 'Explicit' list contains all tests which should be skipped when ran as a larger set. These tests require specific conditions, and thus need to be ran individually.
# To add a test to the 'Explicit' list, provide the tests fully qualified name: e.g. ClassNamespace.ClassName.TestName.

######################################################################################
# BUFFERMANAGEMENT.SERVICE
# ------------------------------------------------------------------------------------
# This file is strictly for Tests which are found within the BufferManagement.Service solution.
######################################################################################

# Failing tests here have a strange usage of AssertNotNull within a pre-processor directive, which is different to the original logic. Placing the tests here, so that it can be investigating separately.

Enterprise.BufferManagement.Service.Test.DistributedCacheTest.TestCacheOperationDoNotCrash
Enterprise.BufferManagement.Service.Test.DistributedCacheTest.TestThreadSafe    # This wasn't failing, but contains the same AssertNotNull change, so I think it's worth sidelining, too.