using WTG.StaticAnalysis.Annotation;

namespace CargoWise.Data.Providers.Common
{
	[CodeAlive("Chunked work WI00891407")]
	public enum SqlDataType
	{
		varbinary,
		binary,
		image,
		varchar,
		@char,
		nvarchar,
		nchar,
		text,
		ntext,
		uniqueidentifier,
		rowversion,
		bit,
		tinyint,
		smallint,
		@int,
		bigint,
		smallmoney,
		money,
		numeric,
		@decimal,
		real,
		@float,
		smalldatetime,
		datetime,
		sql_variant,
		table,
		cursor,
		timestamp,
		xml,
		date,
		datetime2,
		datetimeoffset,
		filestream,
		time,
	}
}
