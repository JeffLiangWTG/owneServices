worker_processes  1;
events {
    worker_connections  1024;
}
http {
    upstream xxxxxx {
        server xxxxxx-1;
        server xxxxxx-2;
    }
server {
        listen          80;
        access_log      /dev/null;
        location / {
            proxy_pass      http://xxxxxx;
        }
    }
}