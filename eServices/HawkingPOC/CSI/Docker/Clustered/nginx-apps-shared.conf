worker_processes  1;
events {
    worker_connections  1024;
}
http {
    upstream apps-shared {
        server apps-shared-1;
        server apps-shared-2;
    }
server {
        listen          80;
        access_log      /dev/null;
        location / {
            proxy_pass      http://apps-shared;
        }
    }
}