input {
  jdbc {
    jdbc_connection_string => "jdbc:sqlserver://syd-thub-1.corporate.cargowise.com:1433;user=DevAdmin;password=3hubRock$;databaseName=eHubTransactions;ApplicationName=logstash"
    jdbc_driver_class => "com.microsoft.sqlserver.jdbc.SQLServerDriver"
    jdbc_user => "DevAdmin"

    statement => "select * from eHubInboxMessage"
  }
}

output {
  elasticsearch {
    hosts => ["localhost:9200"]
    index => "ehubinboxmessage"
  }
}