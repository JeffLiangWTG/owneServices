<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>com.cargowise.eservices</groupId>
	<artifactId>maven-build-tools</artifactId>
	<version>1.0.0</version>
	<name>Common Maven build tools</name>
	<description>Contain the code quality check's rules and Commom Pom's resources.</description>
	<packaging>jar</packaging>
	<parent>
		<groupId>com.cargowise.eservices</groupId>
		<artifactId>plugin-versions-pom</artifactId>
		<version>1.0.0</version>
		<relativePath>../plugin-versions-pom.xml</relativePath>
	</parent>

	<properties>
		<bin.directory>../../Bin</bin.directory>
	</properties>
	<build>
		<resources>
			<resource>
				<directory>resources/</directory>
			</resource>
		</resources>
		<plugins>
		  <plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<executions>
					<execution>
						<id>copy-to-bin</id>
						<phase>package</phase>
						<goals>
							<goal>copy</goal>
						</goals>
						<configuration>
							<artifactItems>
			                  <artifactItem>
			                    <groupId>${project.groupId}</groupId>
			                    <artifactId>${project.artifactId}</artifactId>
			                    <version>${project.version}</version>
			                    <type>${project.packaging}</type>
			                  </artifactItem>
							</artifactItems>
							<outputDirectory>${bin.directory}</outputDirectory>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>
