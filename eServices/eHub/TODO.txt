ACR:

- Added eServices NuGet repo to paket so we can get CargoWise.eServices.Authentication.ServiceClient.  We need to DATify that project and add a direct link rather than loading from NuGet.

- Removed Deployment project imports from projects under the following paths.  We need to do something to handle the deplyment logic that these ran.
    - Adapter
    - Configuration
    - Gateway
    - Nudge
    - Database
    - Common
    - Products\CACustoms
    - Products\NZCustoms
    - Products\USCustoms
    - Clients\TRX

- We added a bunch of warning suppressions for BizTalk projects to QGL.  Need to revisit these to see if we can eliminate the warnings, and the need for suppression.  
    See QuickGetLatest\Warnings.cs

- Removed code signing from eHubDelpoymentTools.  We should readdress this the correct way.

- Deploy folder needs major cleanup and overhaul.  Should be reworked as we work on automating deployment.